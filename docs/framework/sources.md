---
rules: [{"pattern":"sources/","scope":["ctxroute"],"rank":536},{"pattern":"frontmatter.js","scope":["ctxroute"],"rank":537},{"pattern":"frontmatter.property.test.js","scope":["ctxroute"],"rank":538},{"pattern":"migrate-to-frontmatter.js","scope":["ctxroute"],"rank":539},{"pattern":"file-differential.test.js","scope":["ctxroute"],"rank":540},{"pattern":"migrate.js","scope":["ctxroute"],"rank":548},{"pattern":"migrate.test.js","scope":["ctxroute"],"rank":549},{"pattern":"migrate.property.test.js","scope":["ctxroute"],"rank":550},{"pattern":"sources-file.test.js","scope":["ctxroute"],"rank":551},{"pattern":"sources-mcp.test.js","scope":["ctxroute"]},{"pattern":"sources-tool.test.js","scope":["ctxroute"]},{"pattern":"mcp-differential.test.js","scope":["ctxroute"]},{"pattern":"frontmatter.test.js","scope":["ctxroute"],"rank":552}]
mode: dumb
confirm: true
rank: 536
---
# sources/*.js + frontmatter.js — moteur unifié (✅ LIVE, fusion TERMINÉE 17/07/2026)

⚠️ EN PROD : la porte unique `doc-inject.js` (matcher `*`) consomme sources/file.js (fichier) ET sources/mcp.js (MCP). Historique/décisions : `REFACTOR-PLAN.md` (statut TERMINÉ). Casser une source = plus de doc injectée pour toute la flotte d'agents.
⚠️ PURS (gate `sources-must-stay-pure` / `frontmatter-must-stay-pure`) : zéro fs/path/process. C'est la CONDITION pour muter sans mutants équivalents, pas un confort de test.
⚠️ AUCUN DIALECTE DE HARNAIS (gate `sources-must-not-know-the-harness`) : une source répond « quels docs ? », elle ne décide RIEN. `permissionDecision`/`hookSpecificOutput` = le rôle de la PORTE. C'est ce qui rend le portage Codex trivial (écrire une porte, pas un moteur).
⚠️ `sources/file.js` = réplique EXACTE de `protect-files.js` (Bash + reconstruction `cd &&`, `apply_patch` Codex — patch dans `input`/`patch`/`command`, `command` = shape RÉEL Codex ≥ 0.144 mesuré 19/07/2026 —, scope sur TOUS les params, `norm()` backslash+casse). Toute « amélioration » sans relancer le différentiel = régression silencieuse sur 532 règles que personne ne relit.
⚠️ ORDRE = rule-major (règles → chemins), dédup = PREMIÈRE règle gagnante. C'est l'ordre parent→enfant de la concaténation. Un Set ou un ordre path-major casse le sens sans rien afficher.
⚠️ `match` accepte chaîne OU liste : mesuré, 98 des 288 docs ont plusieurs patterns. N'accepter qu'une chaîne rejette un tiers du parc.
⚠️ CADENCE : doc MCP = frontmatter `mode:`/`threshold:`/**`driftUnit:`** optionnels (l'auteur propose, la cascade dispose) ; le JSON ne porte QUE du global + `defaults.{source}` (`servers` = subToolParam seulement). Cascade 4 étages, POINT UNIQUE gate.js : entrée > `defaults.{source}` > global > framework.
⚠️ **`note:` (04/08/2026) = commentaire d'AUTEUR, le SEUL champ que le moteur ne lit JAMAIS** — destiné à qui vient MODIFIER la doc (« pourquoi ce mode/ce scope »), invisible à l'injection car le frontmatter entier est retiré du corps (scellé par un test). 🛑 JAMAIS le pourquoi d'un INVARIANT : celui-là reste dans le corps, sinon la règle dérive. Le jour où le moteur la lirait, ce serait de la config déguisée = 2ᵉ vérité.
⚠️ **`enforce:` (05/08/2026) = booléen, REFUSE le geste au lieu de l'informer.** Admis dans les docs fichier ET MCP ET les entrées skill (même mot partout). Cascade IDENTIQUE aux autres — SAUF qu'il n'a PAS d'étage global (un blocage global refuserait le 1er geste de chaque session). ⚠️ `false` EXPLICITE = annule l'héritage de `defaults.{source}` : sans lui, une catégorie passée en enforce serait INDÉSINSCRIPTIBLE. Valeur non booléenne = ROUGE (jamais prise pour un « oui »). Il SUIT la cadence : aucun mode interdit, l'anti-boucle est l'ALTERNANCE (cf porte.md).
⚠️ **GATE DE SYMÉTRIE DU VOCABULAIRE (05/08/2026, `frontmatter.test.js`)** : toute clé de COMPORTEMENT (le vocabulaire moins les opérateurs de matching) doit exister dans les **4 corpus** (doc fichier · doc MCP · entrée skill · `defaults.{source}`), **sinon ROUGE** — sauf entrée dans `ASYMETRIES_JUSTIFIEES` avec une raison écrite. Volet inverse : une justification devenue fausse rougit aussi. **Symétrie par DÉFAUT, exception DÉCLARÉE** : c'est ce qui rend un décalage silencieux IMPOSSIBLE quand on ajoutera des clés. A payé au 1er run (`note` manquait dans `defaults`). Seule asymétrie admise à ce jour : `confirm` (fichier only, INERTE — global à false — et non supporté par Codex ; retrait au backlog).
⚠️ `tool:` = 4ᵉ déclencheur (19/07/2026, sources/tool.js + toolAdapter) : nom EXACT (===) d'un OUTIL NATIF — comble l'angle mort WebFetch/WebSearch (ni chemin ni mcp__, prouvé muet par spawn). JAMAIS substring. toolAdapter RÉUTILISE acc.decls/bodies posés par fileAdapter (zéro relecture corpus — l'ordre file→tool dans ADAPTERS est une DÉPENDANCE) ; dédup docId = file gagne. scope/exclude via file.shouldSkip (source unique).
⚠️ **DÉCLENCHER SUR UN GESTE (une commande), pas sur un LIEU = `tool` (cible l'outil) + `scope` (filtre la commande — `scope` voit TOUS les params).** `match` = CHEMINS uniquement (+ commande du shell POSIX) : il ne verra JAMAIS un `docker run` passé par un autre shell ni par un outil MCP. Recette prouvée par spawn réel le 31/07/2026 sur 4 canaux (shell POSIX, shell Windows, outil MCP distant, lecture de fichier) — sans elle, chaque auteur la redécouvre au prix d'une session.
⚠️ **JOKER `tool: ["*"]` = n'importe quel outil** (31/07/2026) : VALEUR spéciale, pas un opérateur — la base booléenne reste fermée. **Un joker SANS `scope` ni `exclude` est ROUGE** (il s'injecterait à chaque appel). `*` + `exclude` = « tous SAUF X » (sur cet axe, `exclude` porte sur le NOM D'OUTIL). Nom d'outil vide ⇒ ne matche jamais.
⚠️ **`mcp:` est REJETÉ dans une doc de `docs/`** (31/07/2026) : le canal MCP se déclenche par le CHEMIN `docs/mcp/{server}.md`, jamais par une clé. Avant, il était accepté ET inerte — un validateur qui approuve du mort oriente vers la MAUVAISE cause.
⚠️ **Doc muette ⇒ `node explain.js --doc <nom> --tool X --input '{...}'`** : il donne le motif exact (pattern/scope/exclude/corpus/cadence). NE JAMAIS réimplémenter le moteur pour le sonder — c'est ce qui a coûté une session le 31/07 (3 sondes fausses, un faux verdict « le moteur ne lit pas ce param »).
⚠️ `rules:` = 3ᵉ déclencheur, JSON inline par-entrée `{pattern, scope?, exclude?}` — mesuré 16/07 : 31/103 docs multi-règles ont des scopes DIVERGENTS, irreprésentables avec un scope par doc. `rules` + `match`/`scope`/`exclude` = contradiction ROUGE. `rank` : mesuré (39 paires, 0 conflit) puis CONSERVÉ — parent/enfant abandonné (ordre en partie accidentel, cf plan).
⚠️ MIGRATION TERMINÉE (27/07/2026) : les frontmatters sont la SEULE source de règles, lint-corpus COMPRIS. `protected-paths.json` = vérité de l'ANCIEN moteur (protect-files.js, remplacé le 17/07) ⇒ artefact INERTE : NE PLUS y écrire. Double écriture + ses 2 gates de parité RETIRÉS. ⚠️ SANS RAPPORT avec Codex : ses coquilles tournent sur le NOUVEAU moteur, compatibilité intacte.
⚠️ Parser TOTAL : `parse()` ne throw JAMAIS. Un throw = hook mort = plus AUCUNE doc injectée nulle part (un seul .md malformé casserait tout).
⚠️ Clé de frontmatter inconnue = ERREUR, jamais ignorée : `mach:` au lieu de `match:` = doc morte en silence — le bug que ce refactor tue.
Différentiel = `npm run test:differential` (~75 min, 2021 spawns) : gate de BASCULE, JAMAIS de push. Vert prouve l'équivalence du match, jamais que le moment est bon pour basculer.
