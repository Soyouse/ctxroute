---
rules: [{"pattern":"canari.js","scope":["ctxroute"]},{"pattern":"canari-check.js","scope":["ctxroute"]},{"pattern":"canari.test.js","scope":["ctxroute"]},{"pattern":"canari-check.test.js","scope":["ctxroute"]}]
mode: dumb
---
# canari — le SEUL témoin qui regarde l'AUTRE BOUT du tuyau
⚠️ **TOUT LE RESTE SE TESTE SOI-MÊME.** Le doctor spawne NOTRE hook avec NOTRE payload et vérifie NOTRE sortie — aveugle au seul risque restant : que le HARNAIS change d'avis (champs renommés, `additionalContext` plus consommé). Alors les hooks fail-open en silence, le doctor reste VERT, et plus rien n'atteint l'agent.
⚠️ **LE VERDICT = « on a ÉMIS N fois, est-ce ARRIVÉ ? »** — numérateur `[source: …]` dans le transcript (NOTRE marque), dénominateur = `emission-core.compteurEmissions` (NOTRE compteur). UNE seule trace suffit à prouver que le canal vit ; on ne compare JAMAIS reçu vs attendu.
🛑 **LE NUMERATEUR NE COMPTE PLUS `[source:` NU — DEFAUT ㉘ CORRIGE (07/08/2026).** Ce litteral vit AUSSI dans le texte qui en PARLE : les commentaires de `canari.js` et **64 docs du parc sur 386** qui CITENT un fichier source ainsi ⇒ un agent qui LIT une de ces docs (le geste exact de qui ENQUETE sur une injection morte) faisait passer le canari au VERT. ✅ Seule une etiquette de forme EMISE compte (`.md` en suffixe ou prefixe `skill/`) ; une marque sans `]` n'est JAMAIS comptee (la coupure de fenetre FABRIQUERAIT une etiquette). ⚠️ **PAS le fix complet** : 4 docs du parc citent un `.md` — fix total = n'accepter que les etiquettes REELLEMENT emises via `emission-core` (chantier ㉘ bis, chemin chaud de TOUS les agents).
🛑 **NE JAMAIS COMPTER QUOI QUE CE SOIT DANS LE SCHÉMA DU TRANSCRIPT** (07/08/2026). Doc officielle Codex : *« the transcript format isn't a stable interface for hooks and may change over time »*. Le dénominateur venait de `"type":"tool_use"` (dialecte Claude) et le portage prévoyait de deviner l'équivalent Codex — abandonné. Chercher `[source:` reste LÉGITIME : c'est une sous-chaîne qu'on écrit soi-même, pas un champ de schéma.
⚠️ **UNE SEULE COQUILLE POUR LES DEUX HARNAIS** (plus de `MARQUE_APPEL_*`). ✅ **VÉRIFIÉ DANS LES DEUX DOCS OFFICIELLES le 07/08/2026, pas déduit** : `transcript_path` (« Path to conversation JSON ») et `session_id` sont des champs d'entrée COMMUNS chez Claude (`code.claude.com/docs/en/hooks`) comme chez Codex, et Claude admet explicitement le silence (« Exit 0 means success […] stdout is written to the debug log but not shown »). Un harnais qui diffère sur l'une des 3 exige une coquille — jamais un `if`.
🛑 **LE TRANSCRIPT EST ASYNCHRONE — fait officiel, découvert en vérifiant (07/08/2026).** Doc Claude, verbatim : *« The transcript file is written asynchronously and may lag the in-memory conversation, so it may not yet include the current turn's most recent messages when a hook fires »*. ⇒ une injection qui vient d'atterrir peut ne PAS y être encore. **Inoffensif ici** (le retard porte sur les derniers messages d'un tour ; on exige 25 émissions et on lit 2 Mo). 🛑 **CE FAIT INTERDIT de baisser le seuil à 1-2 ou de ne regarder que le tour courant** : on lirait un fichier en retard et on crierait la mort d'un canal vivant. Le seuil n'est pas de la prudence, c'est ce qui rend décidable la lecture d'un fichier asynchrone.
⚠️ **LE FORMAT DU TRANSCRIPT N'EST SPÉCIFIÉ NI CHEZ CLAUDE NI CHEZ CODEX** (vérifié des deux côtés le 07/08). L'ancien comptage de `"type":"tool_use"` était donc un pari des DEUX côtés, pas seulement côté Codex — le retrait était encore plus justifié qu'annoncé.
⚠️ **SANS ÉMISSION, AUCUNE ACCUSATION** : un transcript très actif mais zéro émission ⇒ `indecidable`, silence. Avant le 07/08 l'activité du HARNAIS suffisait à accuser — faux positif sur un utilisateur qui ne touche aucun fichier documenté.
⚠️ **SCOPE = `lib.scopeId(session_id, agent_id)`, identique à la porte** : composer la clé autrement lirait un compteur inexistant ⇒ `indecidable` éternel, muet et vert. Scellé par le test « CONTRAT DE FRONTIÈRE ».
⚠️ **L'ALARME NE PASSE JAMAIS PAR LE TUYAU TESTÉ** : sortie = `state/canari.json`, lue par un afficheur HORS framework. 🔴 Cet afficheur a pointé `Desktop/mcp-doc-hooks` pendant 3 jours après le renommage en `ctxroute` (04→07/08) : verdict écrit pour PERSONNE. **Renommer le dossier ⇒ repasser dans `statusline.js`**, qu'aucun gate du repo ne peut voir.
⚠️ **MUET quand tout va bien** (`etiquette()` rend `''`) — ne JAMAIS y ajouter un « ✅ ok ». **Sur erreur : on n'écrit RIEN** (verdict précédent préservé) ; écrire « vivant » sans mesure fabriquerait du vert.
⚠️ **LECTURE BORNÉE À 2 Mo depuis la FIN** (transcript du parc mesuré à 104 Mo, 524 ms l'intégrale contre 5 ms la queue). **SEUIL = 25 émissions** = taille d'ÉCHANTILLON, pas un délai.
🔴 **FENÊTRE D'AVEUGLEMENT APRÈS COMPACTION — RÉGRESSION ASSUMÉE, DÉCLARÉE (07/08/2026).** `PreCompact` purge `reliquat-`, donc le compteur repart de ZÉRO : le canari est `indecidable` jusqu'à ce que le seuil soit de nouveau atteint. **Avant, le dénominateur venait du transcript, qui SURVIT à la compaction** — on a donc perdu sur cet axe ce qu'on a gagné en indépendance aux formats tiers. ⚠️ **Aucun test ne l'avait vue** (1081 verts, mutation 100 %, doctor 74 ok) : trouvée en instruisant la question « c'est vraiment solide ? ». **Mesure** : 13 compactions réelles, 94→335 injections entre deux ⇒ le seuil est franchi tôt, l'échange tient SUR CE PARC. 🛑 L'invariant scellé n'est PAS « il voit tout » mais **« il ne ment jamais »** : après compaction ⇒ SILENCE, jamais une accusation (test « APRÈS COMPACTION », negative-check prouvé par sabotage en mémoire).
⚠️ **UserPromptSubmit, jamais PreToolUse** (~330 ms par spawn) ; **stdout VIDE et exit 0 TOUJOURS** — un `exit≠0` y bloquerait le prompt.
⚠️ **CÂBLÉ SUR LES DEUX HARNAIS et surveillé par le doctor** : probe 9 (les DEUX verdicts exigés) + câblage Claude + câblage Codex, negative-checks 3i/3j/5f/7f. Un dead-man switch que personne ne regarde est PIRE que rien : il fabrique de la confiance.
