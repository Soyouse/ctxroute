---
match: [emission-doublon.test.js, alarme-capacite.test.js]
mode: dumb
---
# BANDE PASSANTE D'UN GESTE — pourquoi N trames, et pourquoi PAS une seule
🛑 **JUGEMENT RENVERSÉ EN 24 H, RÉÉCRIT (07/08/2026).** Cette doc a prôné UNE seule déclaration de porte. **C'était faux**, et le retour aux 12 est fait. Motif du renversement : l'exigence n'a jamais été l'ordre d'affichage, mais que **le contexte soit COMPLET avant le prochain appel d'outil**. À 1 trame la capacité tombe à ~7 661 c ⇒ un skill de 53 830 c s'étale sur 8 gestes ⇒ **l'agent agit 7 fois avec un savoir partiel**. Un désordre se recolle (`k/N`), un savoir absent au moment d'agir, non.
🔴 **LA MESURE QUI AVAIT SERVI À JUSTIFIER LE RETRAIT ÉTAIT BIAISÉE** : « les 12 trames n'ont saturé qu'1 fois sur 74 gestes » comptait les trames UTILISÉES. Le bon observable était le **compteur de docs différées, qui ne redescendait plus à zéro** — un corpus `dumb` est redécidé à CHAQUE geste, donc sous-dimensionner N met la file en rotation perpétuelle (déjà écrit dans `porte.md`, jamais relié à l'observation).
⚠️ **L'ORDRE N'EST GARANTI PAR AUCUN HARNAIS, et ce n'est pas rattrapable** — doc officielle relue le 06/08/2026 : *« All matching hooks run in parallel »*, ordre d'agrégation **non spécifié**, et le plafond de 10 000 c s'applique aux **5 types de handler** (`command`/`http`/`mcp_tool`/`prompt`/`agent`). ⇒ ordre garanti ⟺ sortie UNIQUE ⟺ contenu ≤ une trame. 🛑 Ne JAMAIS coder de chaîne de précédence entre processus pour forcer l'ordre : ce serait parier sur un comportement non documenté (interdit par `budget.md`).
⚠️ **CAPACITÉ MESURÉE 07/08/2026** : 1 trame = **7 661 c** · 12 trames = **91 932 c** · charge réelle au pire = **65 265 c (71 %)** dont **53 830 c pour le seul skill**. Au-delà, rien n'est perdu (file) mais l'agent aura agi avant la fin de la livraison.
⚠️ **AUGMENTER N SE PAIE** : chaque trame est un processus lancé à CHAQUE appel d'outil, **même vide** (~330 ms) — 12 ⇒ ~4 s, 50 ⇒ ~17 s. Piste non explorée pour s'en affranchir : les handlers `http` ne lancent aucun processus, mais exigent un daemon (le framework n'en a aucun aujourd'hui).
🛑 **LE « DOUBLON » N'A JAMAIS EXISTÉ — FAUX DIAGNOSTIC, RÉFUTÉ PAR LA MESURE (07/08/2026).** On a cru voir le morceau 7/8 du skill livré 2× (sceaux `2bc5f3df` puis `03d7e9f2`) et on en a déduit une course entre les N processus sur la file. **FAUX** : le transcript montre un **hook `PreCompact` entre les deux** (06:24:24). La compaction PURGE les états ⇒ le skill (`once`) redevient à livrer et se réinjecte ENTIER. **C'est le comportement CONÇU** — sans lui, l'agent repartirait sans son skill après chaque compaction. Reproduction tentée le 07/08 (12 processus réellement parallèles, 2 gestes, 105 segments en file) : **0 doublon**, la file drainait proprement `#12/23` → `#13/23`.
⚠️ **LA LEÇON, ELLE, EST À GARDER** : deux occurrences d'un même identifiant NE SONT PAS un doublon tant qu'on n'a pas regardé CE QU'IL Y A ENTRE LES DEUX. Le fait manquant (la compaction) était à trois lignes dans le transcript. Une observation non revérifiée s'est durcie en certitude à force d'être recopiée dans le code, les docs et le backlog — et a servi d'argument principal pour supprimer une capacité qui marchait. **Avant de graver un défaut : le REPRODUIRE.**
