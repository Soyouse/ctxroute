---
rules: [{"pattern":"gate.js","scope":["ctxroute"]},{"pattern":"gate.test.js","scope":["ctxroute"]},{"pattern":"gate.property.test.js","scope":["ctxroute"]},{"pattern":"doc-inject.js","scope":["ctxroute"]},{"pattern":"porte-core.js","scope":["ctxroute"]},{"pattern":"codex-doc-inject.js","scope":["ctxroute"]},{"pattern":"codex-doc-inject.test.js","scope":["ctxroute"]},{"pattern":"source-adapters.js","scope":["ctxroute"]},{"pattern":"doc-inject.test.js","scope":["ctxroute"]},{"pattern":"corpus.js","scope":["ctxroute"]},{"pattern":"session-store.js","scope":["ctxroute"]},{"pattern":"porte-differential.test.js","scope":["ctxroute"]},{"pattern":"turn-count.js","scope":["ctxroute"]},{"pattern":"turn-count.test.js","scope":["ctxroute"]}]
mode: dumb
---
# gate.js / doc-inject.js / corpus.js / session-store.js — PORTE UNIQUE (LIVE 17/07/2026)

⚠️ `doc-inject.js` = HOOK UNIQUE PreToolUse (matcher `*`) : injecte TOUTES les docs — fichier (frontmatters, sources/file.js) ET MCP (docs/mcp/, sources/mcp.js). `legacy-mcp-inject.js` RETIRÉ du câblage — le recâbler = double injection, le doctor hurle.
⚠️ **COLLECTE = `collect-core.js` (SOURCE UNIQUE, 31/07/2026)** : la porte ET `explain.js` collectent par le MÊME code. Reconstruire un accumulateur ailleurs = l'outil d'introspection décrirait un moteur qui n'existe pas — le bug qu'il prévient.
⚠️ EXTENSION = `source-adapters.js` (registre, contrat documenté en tête) : nouvelle source = 1 module PUR dans sources/ + 1 adaptateur au registre — JAMAIS éditer doc-inject.js/gate.js pour ça. docId unique inter-sources (préfixe dédié).
⚠️ `gate.js` = PUR (muté Stryker 100%) : décision par DOC (dumb/once/smart, compteurs étrangers, threshold PAR DOC via decl). Corpus fichier 100% dumb ⇒ AUCUNE écriture d'état.
⚠️ PARITÉ scellée par `porte-differential.test.js` (fichier, octet) ET `mcp-differential.test.js` (MCP, vieux vs nouveau). Toute modif de format de sortie = relancer LES DEUX.
⚠️ **CASCADE 4 ÉTAGES, POINT UNIQUE = gate.js** (04/08/2026) : entrée > `defaults.{source}` > global (mode/defaultThreshold/defaultDriftUnit) > défaut FRAMEWORK. Fallback TOTAL à chaque étage. L'étage `defaults.{source}` n'existe QUE parce que `decide()` reçoit `owners` (= `acc.owner`, posé par l'adaptateur) : le retirer rendrait l'étage MUET tout en le laissant valide au schéma. Clés admises = les `id` d'ADAPTERS, DÉRIVÉES (config-gate) — `session` n'en est pas (aucune cadence, cf skill §2bis).
⚠️ `driftUnit` (18/07/2026) : unité du compteur smart, `tool` (défaut framework) | `turn` — même cascade, jamais recopiée ailleurs. `turn` = capteur `turn-count.js` (UserPromptSubmit, store 'turn-count-', MUET par contrat : stdout = contexte injecté). Reset des 3 stores par ctxroute-reset.js. decide() exige un turnCount ENTIER (0 si inconnu) — la porte le lit paresseusement (seulement si une doc matchée est en 'turn').
⚠️ **DOCTRINE SCOPE PAR AGENT (19/07/2026)** : maître et CHAQUE sous-agent = contextes DISTINCTS ⇒ état once/smart/turn keyé `lib.scopeId(session_id, agent_id)` — JAMAIS session_id seul (partagé maître/sous-agents, contrat harnais : trou prouvé, sous-agents privés des skills). Sans agent_id = clé historique (rétro-compat Codex). Composer la clé UNIQUEMENT via scopeId. Reset maître = purge par PRÉFIXE session (sous-agents inclus) ; reset avec agent_id = ciblé. Scellé tests « SOUS-AGENT » de doc-inject.test.js.
⚠️ **PORTAGE (19/07/2026)** : corps commun = `porte-core.js` (SOURCE UNIQUE, run(data, emit)) — les coquilles ne gardent QUE leur dialecte de sortie (~15 l.). Claude = doc-inject.js (allow/deny) · Codex = codex-doc-inject.js (SANS permissionDecision hors deny). Toute évolution d'orchestration → porte-core.js, JAMAIS une coquille. reset/turn-count/session-inject se câblent TELS QUELS côté Codex (dialecte identique).
⚠️ **`confirm`/`ask` RETIRÉS le 05/08/2026** — 3 décisions et pas 4 : `none` · `allow` · `deny`. `ask` escaladait vers l'HUMAIN (anti 0-human), n'existait pas sur Codex, et `enforce` couvre le besoin en mieux. `WRITE_TOOLS` part avec (aucune décision ne dépend plus du NOM de l'outil). Anti-retour scellé par spawn RÉEL sur les 2 coquilles. La porte ne lit JAMAIS `.rush` non plus. `corpus.js`/`session-store.js` = I/O PARTAGÉE avec shadow-inject.js (RELIQUE décâblée 17/07/2026 — ne PAS recâbler) : toute modif = re-prouver par leurs suites + doctor.
⚠️ **`enforce` (05/08/2026) — la porte peut ARRÊTER le geste.** Motif : doc officielle, l'`additionalContext` d'un PreToolUse arrive « next to the tool result » ⇒ une injection ne peut PAS empêcher le geste qu'elle vise, seulement le suivant. `enforce: true` ⇒ décision `deny`, la doc part en `permissionDecisionReason` (JAMAIS en additionalContext : il arriverait après). MÊME JSON sur les 2 harnais (vérifié dans le binaire Codex 0.144.6). Reste l'EXCEPTION : absent = comportement d'avant à l'octet. ⚠️ Ce n'est PAS une sécurité mais un GARDE-FOU : la porte est fail-open, un hook mort laisse passer.
⚠️ **ANTI-BOUCLE = ALTERNANCE, jamais un interdit de mode** : un blocage n'est JAMAIS suivi d'un blocage (drapeau `denied` dans l'état) ⇒ le geste refait passe TOUJOURS, puis la cadence reprend. Les 3 modes sont donc légitimes (`dumb` = bloque/passe/bloque). ⚠️ Une doc `enforce` écrit son état MÊME en `dumb` (sans quoi rien ne se souvient du refus) ; sans `enforce`, shape d'état INCHANGÉE — parité intacte.
⚠️ **COUVERTURE D'OUTILS CODEX — doc officielle relue 05/08/2026 (`learn.chatgpt.com/codex/hooks`)** : le « code mode » (JS appelant `tools.shell_command`) ne contourne PAS les hooks — *« When a model uses code mode to call a tool from JavaScript, hook decisions apply to that nested call »* ⇒ le hook reçoit l'appel IMBRIQUÉ avec son `tool_name` canonique, JAMAIS le JavaScript. Shell et unified exec (`exec_command`) se matchent **comme `Bash`** (`tool_input.command`) — `sources/file.js` est conforme SANS changement. Ne PAS « corriger » le moteur pour du code mode.
⚠️ **DEUX TROUS DE COUVERTURE CÔTÉ CODEX, par contrat du harnais (05/08/2026)** : ① les outils HÉBERGÉS (`WebSearch`…) *« don't use the local function-tool hook path »* ⇒ un déclencheur `tool: ["WebSearch"]` est MUET sur Codex (asymétrie de HARNAIS, pas un bug de chez nous — ne pas la chasser dans le moteur) ; ② *« Some specialized tool paths can opt out of the default hook path. Treat tool hooks as a useful guardrail, not a complete enforcement boundary »* ⇒ `enforce` reste un GARDE-FOU, jamais une frontière d'application. `write_stdin` ne rejoue PAS `PreToolUse`.
⚠️ **L'ORACLE DU DIFFÉRENTIEL EST DATÉ — comparer le CONTENU, pas l'enveloppe (05/08/2026)** : `protect-files.js` est FIGÉ (sa doc lui interdit toute évolution) et ignore le SCEAU né le 03/08. Dès qu'une injection dépasse 50 % du budget (4 000 c) la porte scelle, l'oracle non ⇒ l'octet BRUT diverge toujours, sans qu'aucun moteur ait changé. `porte-differential.test.js` déscelle donc avant de comparer (back-référence sur le marqueur + negative-check dédié). 🛑 NE JAMAIS faire passer ce rouge en RACCOURCISSANT une doc : ce serait dégrader un livrable pour tenir dans NOTRE plomberie.
⚠️ **FILE D'ÉMISSION `reliquat-` (05/08/2026) — c'est elle qui rend « tout arrive » VRAI.** Ce qui ne tient pas dans les N trames d'un geste est PERSISTÉ et ré-émis au geste suivant, **en TÊTE** (RFC 6455 : un document fragmenté n'est JAMAIS entrelacé — la file passe avant le frais, sans quoi le recollage devient impossible). **DÉDUP obligatoire par doc de base** (`doc#3` → `doc`) : une doc `dumb` est re-décidée à chaque geste et serait ré-empilée derrière ses propres morceaux. **On stocke le TEXTE, pas des ids** : les N processus doivent voir la même entrée (le plan mémoïse donc les SEGMENTS) et une doc éditée en cours de route ferait sinon recoller deux versions. **Sortie de boucle** : tester `segments.length`, JAMAIS `r.inject.length` — la file peut porter du contenu alors que `gate.decide` n'a rien décidé de neuf. Sans lock : ni lecture ni écriture de la file (on dégrade au frais, jamais une corruption).
⚠️ **LA FILE NE SE VIDE QUE POUR UN CORPUS QUI CESSE D'ÊTRE REDÉCIDÉ** (`once`/`smart`). Un corpus `dumb` durablement au-dessus de la capacité livre **en ROTATION, indéfiniment** — c'est CORRECT (`dumb` = « réinjecte à chaque geste »), rien n'est perdu, mais ne pas attendre une file vide et ne pas « corriger » ce cas. Mesuré 05/08/2026.
⚠️ **LA BOUCLE DE RESTAURATION D'ÉTAT DES DIFFÉRÉS A ÉTÉ SUPPRIMÉE** avec la file — et NE DOIT PAS revenir. Elle « dé-marquait » une doc différée pour qu'elle soit redécidée, parce qu'un différé était alors PERDU. Désormais il est EN VOL : la garder livrerait la doc deux fois (file + redécision). La garantie « jamais consommée sans être livrée » n'a pas disparu, elle a changé de gardien (property ⑧ de `budget.property.test.js`).
⚠️ **PAQUETS (03/08/2026)** : `--paquet k --paquets N` déclarés N fois dans settings.json (MÊME script ; la dédup se fait par commande + args, donc des indices différents ne fusionnent pas). La COQUILLE passe l'indice et `invocationId` (`tool_use_id`) — le noyau ne lit JAMAIS un champ de harnais. **Plan MÉMOÏSÉ par invocation** (store `plan-`, clé préfixée session, purgée par ctxroute-reset) : sans lui chaque processus rappelle `gate.decide` qui ÉCRIT l'état ⇒ le 1er consomme les `once`, les paquets suivants sont VIDES. Déclaration incomplète ⇒ trame unique INTÉGRALE, jamais un découpage bancal.
