---
match: [doctor.js, doctor.test.js]
scope: [ctxroute]
mode: dumb
rank: 356
---
# doctor.js / doctor.test.js — dead-man switch

⚠️ RAISON D'ÊTRE : un hook PreToolUse mort est INDISCERNABLE d'un hook absent (aucune erreur, aucun log — juste plus de doc injectée). Déjà vécu 2× le 15/07/2026. Câblé en SessionStart (`--quiet --settings ...`) : silencieux si vivant, hurle si mort.
NE JAMAIS le rendre fail-open comme les hooks : un hook DOIT être silencieux/non-bloquant, un DIAGNOSTIC DOIT hurler (exit≠0 + stderr). Rôles opposés, jamais fusionnés.
⚠️ Toute sonde prouve un EFFET RÉEL (contexte injecté, store incrémenté, stores EFFACÉS), JAMAIS un simple exit 0 — trou fermé 19/07/2026 : le reset était vérifié « sort sans crash » ; un stub exit-0-muet était indiscernable d'un reset vivant (negative-check 3f le prouve). Nouvelle sonde = poser les 3 stores → reset → exiger l'ABSENCE.
NE JAMAIS supprimer les cas NÉGATIFS de `doctor.test.js` (sabotage réel sur COPIE tmpdir) : un dead-man switch jamais déclenché = fausse confiance. Le vert sur repo sain ne prouve RIEN.
`--quiet` ne réduit QUE la sortie de succès — un échec hurle toujours, quiet ou pas.
Le probe s'isole via `CTXROUTE_CONFIG_PATH`/`DOCS_DIR`/`STATE_DIR` (tmpdir) : il ne DOIT jamais toucher les fichiers livrés (cas 6 le vérifie).
Le check `--settings` est la SEULE couverture du câblage : il vit hors du repo, aucun test du repo ne peut le voir.
⚠️ **BANDE PASSANTE — 4 CHECKS DANS `--settings` (07/08/2026)** : ① toutes les déclarations annoncent le MÊME `--paquets` · ② autant de déclarations que de trames annoncées · ③ indices `--paquet` couvrant **1..N sans trou ni doublon** (un indice manquant = une trame jamais émise ; en double = un contenu livré 2× — **les deux SILENCIEUX**) · ④ **égalité avec `paquets` de `ctxroute-config.json`**. 🔴 Le ④ existe parce que le même chiffre vit à DEUX endroits (config = intention, settings = ce que le harnais exécute) et que le moteur ne peut pas imposer le second : c'est la divergence silencieuse payée le 05/08 (skill livré en 11 gestes au lieu d'1, 995 tests verts). Clé absente de la config ⇒ aucun reproche (on n'impose pas de déclarer sa bande passante). 🛑 Ces checks ont REMPLACÉ deux checks du 06/08 qui exigeaient UNE seule déclaration et refusaient `--paquets N>1` — **prémisse fausse, annulée** (cf `paquet-unique.md`) : ne pas les ressusciter.
⚠️ CODEX (19/07/2026) : probes 7-8 = coquilles codex-doc-inject/codex-doc-write-guard (mêmes preuves d'effet réel) ; `--codex-hooks <path>` = câblage Codex (5 voies + fichiers = CE repo + anti-DOUBLE injection : protect-files.js ne doit PLUS y être câblé). Negative-checks 3g/3h/7 dans doctor.test.js — jamais supprimés.
⚠️ **PLAFOND DE CONTEXTE CODEX (04/08/2026)** : les 2 ÉMETTEURS (`codex-doc-inject`, `session-inject`) DOIVENT déclarer `additionalContextLimit = 0` dans le câblage — omis = défaut 2500 TOKENS, spill disque + aperçu EN SILENCE (les gros skills n'arrivaient jamais entiers). Vérifié PAR BLOC (un match global laisserait l'autre émetteur muet) et motifs à guillemets OPTIONNELS (TOML `=` ET JSON `":`) — sans ça le check est inerte en JSON. Ne l'exiger QUE des émetteurs : l'imposer au reset/garde/compteur serait une déclaration inerte. Negative-check 7d (4 volets, dont « un seul émetteur réglé »).
⚠️ **PROBE 9 = LE CANARI (05/08/2026)** : spawn réel sur transcript FABRIQUÉ, verdict lu dans state/canari.json. **Les DEUX verdicts sont exigés** (mort sans injection, vivant dès UNE injection) — un seul cas laisserait passer un canari figé sur une constante, exactement le piège des gates de pureté inertes. Negative-checks 3i (muet) / 3j (FIGÉ sur vivant) / 5f (non câblé) — jamais supprimés.
⚠️ **LA SONDE DOIT POSER LE DÉNOMINATEUR (07/08/2026)** : elle écrit `reliquat-doctor-probe.json` avec `emissions: 30` AVANT de lancer le canari. Les lignes `"type":"tool_use"` du transcript fabriqué ne sont plus que du BRUIT — plus rien n'est compté dans le schéma du transcript (doc Codex : *« isn't a stable interface »*). Sans ce fichier, le canari répond `indecidable` et la sonde ne prouve RIEN.
⚠️ **6ᵉ VOIE CODEX = LE CANARI (07/08/2026, chantier ② fermé)** : `canari-check.js` est exigé dans `--codex-hooks` — MÊME fichier que côté Claude, sans coquille (`transcript_path`/`session_id` documentés des deux côtés). 🛑 Sans lui, Codex est aveugle à sa propre mort, et le framework perd son SEUL gate anti-dépréciation praticable (les 3 autres pistes mesurées et fermées le 05/08). 🛑 Ne PAS lui réclamer `additionalContextLimit` : il n'émet aucun contexte, la déclaration serait inerte. Negative-check **7f** (exit≠0 + le canari NOMMÉ) — jamais supprimé.
⚠️ **CODEX 0.146.0 — DOC OFFICIELLE RELUE LE 05/08/2026** (learn.chatgpt.com/docs/hooks + /config-file/config-basic). ① Le flag est **`[features].hooks`** (Stable, « Enable lifecycle hooks from hooks.json or inline [hooks] ») — **`codex_hooks` N EXISTE PLUS** dans la liste officielle : renommé dans config.toml le 05/08. ② **CONTRAT INCHANGÉ** : `hookSpecificOutput` + `additionalContext` + `permissionDecision: deny` fonctionnent comme avant — notre `enforce`/deny reste valide. **`ask` N EST PAS dans la doc** (seuls `deny` et `allow`, ce dernier avec `updatedInput` qui RÉÉCRIT l appel) : le retrait de `ask` du framework est donc aligné sur Codex, pas seulement sur le 0-human. ③ `additionalContextLimit` : **tokens, défaut 2500, `0` = intégral** — INCHANGÉ, et enfin ACTIF (0 → 18 occurrences dans le binaire entre 0.144.6 et 0.146.0). ④ **11 événements** dont **PostCompact · SubagentStart/Stop · PermissionRequest · SessionEnd · Stop** = voies NON exploitées par le framework (PostCompact notamment : côté Claude on ne tient que PreCompact). ⑤ ⚠️ **ÉCART À NE PAS FONDRE** : la doc cite `hooks.json` comme source valide, alors que notre terrain 0.144 l avait mesuré IGNORÉ — re-mesurer avant de s y fier. ⑥ 🛑 **Le « code mode » N EST PAS DOCUMENTÉ** (outil `exec` recevant du JavaScript `tools.shell_command({...})`, observé en run réel 0.146) : aucun flag documenté ne le décrit (`unified_exec` = « unified PTY-backed exec tool », défaut true SAUF Windows ; `shell_tool` = « default shell tool », true). Non documenté ⇒ **MESURER** (capturer le payload réel), jamais rétro-ingénierer à l aveugle. Cf REFACTOR-PLAN ⑨.
⚠️ **FEATURE FLAG CODEX — `--codex-config <config.toml>` (05/08/2026)** : opt-in SÉPARÉ de `--codex-hooks`, car le flag vit dans un AUTRE fichier que le câblage managé. Exige `hooks = true` PRÉSENT **ET** `codex_hooks` (déprécié 0.146) ABSENT — les deux, jamais un seul. Ancrage début de ligne : un COMMENTAIRE a le droit de nommer `codex_hooks` (même leçon que le faux positif protect-files). Sans ce check, le meilleur câblage du monde est mort en silence. Negative-check 7e (5 volets).
⚠️ TERRAIN Codex 0.144 (mesuré 19/07/2026) : `~/.codex/hooks.json` IGNORÉ. Câblage framework = `C:\ProgramData\OpenAI\Codex\requirements.toml` (hooks MANAGÉS = trustés d'office par politique, ZÉRO /hooks — prouvé sans bypass) ; pointer --codex-hooks dessus. Hooks perso (rush-banner/sync-mcps) restent dans config.toml — ne JAMAIS y recâbler le framework (double injection). Payload réel : tool_name="Bash" même pour shell_command.
